$(".slide").on('ajax:success', '#movie<%= @movie.id %>', function () {

  // DOM書き換え
  $('.movie-player .html').html('<%= @movie.movie_html.html_safe %>');
  $('.movie-player .title').html('<%= @movie.title %>');
  $('.movie-player .name').html('<%= @movie.player.name %>');

  // スクロールトップ
  var position = $(".movie-box .head-title").offset().top;

  $("html,body").animate({
      scrollTop : position
  }, {
      queue : false
  });
});