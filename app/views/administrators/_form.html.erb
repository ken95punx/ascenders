<%= form_with(model: administrator, local: true) do |form| %>
  <% if administrator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(administrator.errors.count, "error") %> prohibited this administrator from being saved:</h2>

      <ul>
      <% administrator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :administrator_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :administrator_email %>
  </div>

  <div class="field">
    <%= form.label :crypted_password %>
    <%= form.text_field :crypted_password, id: :administrator_crypted_password %>
  </div>

  <div class="field">
    <%= form.label :salt %>
    <%= form.text_field :salt, id: :administrator_salt %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
